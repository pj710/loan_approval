"""Fairness metrics for mortgage decision models."""


def demographic_parity_difference(y_true, y_pred, sensitive_feature):
    """Return a placeholder fairness metric."""
    return {"sensitive_feature": sensitive_feature, "metric": None}
